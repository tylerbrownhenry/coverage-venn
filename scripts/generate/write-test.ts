import * as fs from "fs"; import * as path from "path"; const testFilePath = path.resolve(__dirname, "../../coverage-project/test-simple.html"); const testContent = `<!DOCTYPE html><html><head><title>Test</title></head><body><h1>Test</h1><p>Generated at: ${new Date().toISOString()}</p></body></html>`; try { fs.writeFileSync(testFilePath, testContent); console.log(`Test file written to: ${testFilePath}`); } catch (error) { console.error("Error writing test file:", error); }
