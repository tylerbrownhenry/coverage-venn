import * as fs from "fs"; import * as path from "path"; console.log("Starting simplified report generation"); const OUTPUT_DIR = path.resolve(process.cwd(), "../../coverage-project"); const HTML_OUTPUT_PATH = path.resolve(OUTPUT_DIR, "simplified-report.html"); try { if (!fs.existsSync(OUTPUT_DIR)) { fs.mkdirSync(OUTPUT_DIR, { recursive: true }); } const html = `<!DOCTYPE html><html><head><title>Simplified Report</title></head><body><h1>Simplified Report</h1><p>Generated at: ${new Date().toISOString()}</p></body></html>`; fs.writeFileSync(HTML_OUTPUT_PATH, html); console.log(`Report written to: ${HTML_OUTPUT_PATH}`); } catch (error) { console.error("Error generating report:", error); }
