console.log("Reading coverage files..."); const fs = require("fs"); const path = require("path"); const COVERAGE_ANALYSIS_DIR = path.resolve(process.cwd(), "../..", "coverage-analysis"); const COMPONENT_COVERAGE_PATH = path.resolve(COVERAGE_ANALYSIS_DIR, "project-component-coverage.json"); try { const componentCoverage = JSON.parse(fs.readFileSync(COMPONENT_COVERAGE_PATH, "utf8")); console.log(`Read ${componentCoverage.length} components`); } catch (error) { console.error("Error reading component coverage:", error); }
